@inject NavigationManager Nav

@code {

    protected override void OnInitialized() {
        base.OnInitialized();
        if (Nav.Uri != $"{Nav.BaseUri}{WebConst.AppRegister}") {
            Nav.NavigateTo(WebConst.AppRegister);
        }
    }

}